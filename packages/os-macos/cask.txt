# macOS casks installed after formulae (mirrors pacman-then-AUR ordering).
# Ghostty is the standard terminal on managed Macs; do not add a second
# managed terminal here.
ghostty
nikitabobko/tap/aerospace
rectangle
raycast

# wallpaper setter used by dotfiles-wallpaper (task 17)
desktoppr

# per-device scrolling: trackpad stays natural, mice get classic wheel
# direction via stowed ~/.config/linearmouse/linearmouse.json
linearmouse

# fonts
font-jetbrains-mono-nerd-font
font-fira-code-nerd-font

# zen: Zen.app was installed manually on lamac; `brew install --cask zen`
# fails over an existing app. To adopt it into brew later, run:
#   brew install --cask --adopt zen
# zen
