# Global base stow packages — applied for every profile on every OS,
# including `minimal`. These are the shared terminal/shell dotfiles.
# One package per line; `#` comments and blank lines are ignored.
shell
zsh
bash
git
ssh
tmux
scripts
atuin
