# macOS work laptop — desktop + work extras
# Shared dotfiles: profiles/stow-base. macOS shortcuts: profiles/stow-os-base.
PACKAGE_GROUPS=(common desktop work)
STOW_PACKAGES=(nvim btop wezterm)
SERVICES=(borders)
SYNC=(atuin)
