# Optional Omarchy input feature: make ALT behave as SUPER/Windows.
# Enable per profile in profiles/profile-<hostname>-omarchy/stow-layers.txt.

input {
    kb_options = compose:caps,altwin:swap_alt_win
}
