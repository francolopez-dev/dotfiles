# fornax (work Lenovo) pacman packages, on top of global + os-omarchy.

# Audio firmware/tooling for Lenovo Legion Pro 7 ALC287 speaker diagnostics.
sof-firmware
alsa-tools

# Local LLM tooling for FORNAX only.
# Ollama itself is currently declared in aur.txt as binary packages because the
# Arch extra/ollama-cuda package can lag upstream model manifests.
lmstudio-bin
