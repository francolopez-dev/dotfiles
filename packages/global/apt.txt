# Global apt packages, installed on every Debian/Ubuntu machine.
git
stow
tmux
fzf
ripgrep
jq
bat
eza
fastfetch
btop
htop
ncdu
git-delta
direnv
tealdeer
neovim
zsh
curl
bash
wget
nano
# Terminal basics available in stock Debian/Ubuntu repos.
# fd-find installs the binary as `fdfind` (bat as `batcat`).
bash-completion
tree
rsync
unzip
fd-find
file
# Provides infocmp/tic/tput for terminfo handling; the shell fallback and
# Ghostty ssh-terminfo both need it.
ncurses-bin
# Yazi is not a stock Debian/Ubuntu apt package here. dotfiles update manages
# it separately from the official upstream GitHub .deb on Debian/Ubuntu only.
