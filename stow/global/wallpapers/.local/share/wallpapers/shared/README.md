# Shared Wallpapers

Git-tracked wallpapers in this directory are shared by every managed OS.

Rules:
- Commit only curated `jpg`, `jpeg`, `png`, or `webp` images.
- Keep each image at or below 8 MB.
- Keep private or personal images in `~/Pictures/local-wallpapers` instead.

Git history keeps images forever, so be selective.
