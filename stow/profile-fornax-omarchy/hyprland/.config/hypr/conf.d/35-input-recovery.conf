# Emergency recovery for intermittent session-level input stalls.
bindd = SUPER SHIFT, ESCAPE, Reset input stack, exec, sh -lc 'voxtype record cancel; hyprctl dispatch submap reset; pkill fcitx5; uwsm-app -- fcitx5 --disable notificationitem'
