# fornax (work Lenovo) AUR packages.

# Binary upstream Ollama packages for newer model manifest support when
# extra/ollama-cuda lags upstream. Keep these binary-only; do not use ollama-git.
ollama-bin
ollama-cuda13-bin
