# assets

Single source of truth for shared, non-secret binary/static assets.

- `wallpapers/` — desktop wallpapers
- `themes/` — color themes shared across tools

These are referenced by stow packages and the desktop setup; they are not
symlinked wholesale.
